{
  "name": "star-flow-command",
  "version": "1.0.0",
  "description": "Star Flow Command — 2D space strategy game (PWA)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "build": "npx tsc && npx vite build",
    "preview": "npx vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.2.0"
  }
}
